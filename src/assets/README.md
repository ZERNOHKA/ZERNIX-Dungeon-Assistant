# Арт ZERNIX — единая папка

**Используйте только эту папку** для картинок, которые нужны приложению.

| Файл / папка | Назначение |
|--------------|------------|
| `logo-dragon.png` | Логотип в левом сайдбаре |
| `publicd20_dice.png` | Кубик d20 внизу сайдбара |
| `backgrounds/<раздел>/` | Фоны по разделам (см. `backgrounds/README.md`) |

Подключение — через `src/lib/zernixArt.ts` (логотип, d20) и `src/lib/sectionBackgrounds.ts` (фоны).

## Не дублируйте файлы

| Папка | Статус |
|-------|--------|
| **`src/assets/`** | ✅ Рабочая — сюда кладите файлы |
| `public/assets/` | ⚠️ Старые копии, приложение **не использует** |
| `assets/` (корень) | 📁 Черновики / референсы, **не подключаются** |

После переноса файлов в `src/assets/` дубликаты из `public/assets/` можно удалить.

## Устаревшие файлы

- `dragon-bg.png` — старая копия фона; используйте `backgrounds/home/` вместо него.
