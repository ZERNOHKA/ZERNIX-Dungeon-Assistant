# Legacy Mobile UI

Экраны и компоненты **первой версии** интерфейса (Tailwind, mobile-first, `screens/*`).

Не импортируются из `src/App.tsx`. Активный UI — `src/zernix/views/`.

Сохранено для истории учебной практики и возможного сравнения подходов.
